# This is a template for production environment variables on Railway
# Set these values in Railway dashboard under Environment Variables

# DATABASE_URL is required - should be your MySQL connection string
# DATABASE_URL=

# JWT_SECRET should be a long, random string (at least 32 characters)
# Generate one with: openssl rand -base64 32
# JWT_SECRET=

# NEXT_PUBLIC_SOCKET_URL should point to your Railway app's socket server
# For Railway, this will be: https://your-railway-app.up.railway.app
# NEXT_PUBLIC_SOCKET_URL=

# Optional: Socket server port (only set if running socket server on different port than Next.js)
# SOCKET_PORT=
